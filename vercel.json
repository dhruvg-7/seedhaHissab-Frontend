{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "env": {
    "BASE_PATH": "@BASE_PATH"
  },
  "rewrites": [
    {
      "source": "/:path*",
      "destination": "/index.html"
    }
  ]
}
